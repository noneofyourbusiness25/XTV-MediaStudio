pyrofork
TgCrypto>=1.2.5
motor~=3.6.0
dnspython~=2.7.0
requests~=2.32.3
python-dotenv~=1.0.1
pymongo~=4.9.1
aiohttp~=3.10.5
guessit
certifi>=2024.7.4
psutil
yt-dlp>=2025.1.15
# Fernet for Mirror-Leech provider credential encryption (Secrets.py)
# and the /admin → Mirror-Leech Config → Generate SECRETS_KEY button.
cryptography>=42.0.0
# Mirror-Leech downloaders
gallery-dl>=1.27.0
feedparser>=6.0.11
# Mirror-Leech optional uploaders (Cloud-Giants).
# Each is gated via python_import_required so hosts that leave the lib
# uninstalled simply never see the provider in /settings menus.
dropbox>=11.36
msal>=1.26
boxsdk>=3.9
boto3>=1.34
b2sdk>=2.1
# Optional: natural-language time parsing for the /ml "Schedule →
# Custom…" flow. When absent, the flow falls back to a handful of
# strptime formats (ISO, German, bare HH:MM) so the feature still works.
dateparser>=1.2
